CREATE TABLE BOOK (
  ID INTEGER PRIMARY KEY,
  TITLE VARCHAR(255) NOT NULL,
  GENRE_ID INTEGER,
  ISBN VARCHAR(13),
  PAGES INTEGER,
  PUBLISHER_ID INTEGER,
  PUBLISHED_DATE CHAR(10),
  LANG VARCHAR(50)
);
