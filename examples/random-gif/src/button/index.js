import { initialModel } from "./model";
import { receive } from "./receive";
import { createActions, intents } from "./actions";

export const button = ({
  initialModel,
  receive,
  createActions,
  intents
});
